name: Cypress Tests
on: [push]
jobs: 
  cypress-run:
    name: Your Display Name Here
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.1.5
      - name: Cypress.io
        uses: cypress-io/github-action@v6.5.0
